# AIMS OS labo — the HP All-in-One 24-f0xx has a discrete NVIDIA chip next
# to the Intel GPU that drives the panel. Initialising it with nouveau at
# boot costs about seven seconds before the login screen appears and
# brings nothing to a lab desktop (GNOME picks the Intel GPU anyway).
blacklist nouveau
options nouveau modeset=0
