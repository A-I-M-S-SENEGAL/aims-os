# AIMS OS labo — images pulled by root (aims-labo-extras) are visible
# read-only to every rootless Podman user, so students start their
# database containers without downloading anything.
[storage]
[storage.options]
additionalimagestores = ["/var/lib/containers/storage"]
