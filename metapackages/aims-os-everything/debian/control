Source: aims-os-everything
Section: metapackages
Priority: optional
Maintainer: AIMS Senegal IT <hakim@aims-senegal.org>
Uploaders: AIMS Senegal IT <hakim@aims-senegal.org>
Build-Depends: debhelper-compat (= 13)
Standards-Version: 4.6.2
Rules-Requires-Root: no
Homepage: https://github.com/A-I-M-S-SENEGAL/aims-os
Vcs-Browser: https://github.com/A-I-M-S-SENEGAL/aims-os
Vcs-Git: https://github.com/A-I-M-S-SENEGAL/aims-os.git

Package: aims-os-everything
Architecture: all
Multi-Arch: foreign
Depends:
 ${misc:Depends},
# Pulls every AIMS OS layer in one shot. Identical install footprint to
# the v2.0 "everything in the ISO" experience, just driven by apt.
# Order from base to leaves.
 aims-os-core,
 aims-os-desktop,
 aims-os-desktop-extras,
 aims-os-desktop-dev,
 aims-os-math,
 aims-os-math-extras,
 aims-os-bigdata,
 aims-os-security,
 aims-os-centre-senegal,
 aims-os-welcome,
# aims-os-branding stays a Recommends rather than a Depends: it is now
# published in the apt repo too (build-debs.sh renders the payload on
# the CI runner), but a vanilla Debian user may legitimately want the
# whole AIMS stack without the AIMS look. apt installs recommends by
# default, so ISO users and default installs still get it.
Recommends:
 aims-os-branding,
Description: AIMS OS — pull every layer (v2.0 "complete install" alias)
 Convenience metapackage for users who want the v2.0 "everything in
 the ISO" experience on top of the slim v2.1 install. Pulls every
 AIMS OS metapackage: core, branding, desktop, desktop extras, desktop
 dev stack, math, math extras, Big Data, Security, centre profile,
 and the first-boot wizard.
 .
 Install with:
 .
   sudo apt install aims-os-everything
 .
 About 9 GB on disk after install. Most students don't need this —
 the slim v2.1 ISO plus a single filière pick (aims-os-bigdata or
 aims-os-security via the first-boot wizard) covers the curriculum.
 .
 aims-os-branding is pulled as a Recommends (installed by default): it
 carries the AIMS look (Plymouth, GRUB theme, wallpapers, os-release),
 which a vanilla Debian user may prefer to skip with
 --no-install-recommends.
